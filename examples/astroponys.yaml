schema_version: 1

images:
  directory: .
  recursive: false

output:
  directory: astroponys-output

identity:
  session_id: ngc-7635-focuscheck
  target_id: NGC 7635
  panel_id: null

focus:
  reference_filter: Luminance
  filter_order: [Luminance, Red, Green, Blue, Halpha, OIII, SII, LPR]

fits:
  header_aliases:
    focus_position: [FOCUSPOS, FOCPOS, FOCUS]
    filter_name: [FILTER, FILTERID]

contact_sheet:
  percentiles: [1.0, 99.5]
